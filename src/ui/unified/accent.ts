/**
 * The one per-game token.
 *
 * Everything else in `src/ui/unified/` is byte-identical across the eleven
 * games; this file is the single place each repo differs. Sourced from this
 * game's existing palette (coin gold) so the shared chrome still feels native
 * to the scene.
 */
export const ACCENT = "#E8B33C";

/** Tint used for an accent-on-surface fill (active rows, selected chips). */
export const ACCENT_SOFT = "rgba(232, 179, 60, 0.14)";
